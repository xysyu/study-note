#inclucde<bits/stdc++.h>
using namespace std;
int find(int n)
{
  if(f[n]==n) return n;
  return f[n]=find(f[n]);//其中f[n]代表n元素的父节点
}
//find()函数用于查找一个元素所在的集合，该元素在‘归’的过程中，还可以顺便将路径中的元素一并指向根节点
void merge(int a,int b)
{
  int a1,b1;
  a1=find(a);
  b1=find(b);
  if(a1==b1) return;
  f[a1]=b1;
}
//merge(合并)函数用于合并两个元素所在的集合
int main()
{
  return 0;
}
